-- Отдельная БД для интеграционных тестов: они дропают и пересоздают схему,
-- поэтому не должны трогать данные, на которых работает приложение.
CREATE DATABASE food_orders_test OWNER food;

-- База для e2e: сценарии создают и меняют заказы, и делать это в базе,
-- которую показывает демонстрационный стенд, нельзя.
CREATE DATABASE food_orders_e2e OWNER food;
